Template.notepad.rendered = function() {
  var game = new Phaser.Game('0', '0', Phaser.CANVAS, 'notepad', { preload: preload, create: create });

  function preload() {

  }

  function create() {

  }
}
